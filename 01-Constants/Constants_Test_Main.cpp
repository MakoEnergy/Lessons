#include <cassert>

// Main
int main()
{
    static_assert(sizeof(float) == sizeof(1.0),"Datatype size mismatch.");
    static_assert(sizeof(float) == sizeof(1.0),"Datatype size mismatch.");
    static_assert(sizeof(float) == sizeof(1.0),"Datatype size mismatch.");
    static_assert(sizeof(float) == sizeof(1.0),"Datatype size mismatch.");
    static_assert(sizeof(float) == sizeof(1.0),"Datatype size mismatch.");
    static_assert(sizeof(float) == sizeof(1.0),"Datatype size mismatch.");
    return 0;
}
